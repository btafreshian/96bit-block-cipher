# SPDX-License-Identifier: MIT
# Research-only cube96 CI workflow enforcing strict compiler warnings.
name: CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build:
    name: ${{ matrix.compiler }} ${{ matrix.build_type }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        compiler: [gcc, clang]
        build_type: [Debug, Release]
    env:
      BUILD_DIR: build/${{ matrix.compiler }}-${{ matrix.build_type }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup compiler
        run: |
          if [ "${{ matrix.compiler }}" = "gcc" ]; then
            echo "CC=gcc" >> "$GITHUB_ENV"
            echo "CXX=g++" >> "$GITHUB_ENV"
          else
            echo "CC=clang" >> "$GITHUB_ENV"
            echo "CXX=clang++" >> "$GITHUB_ENV"
          fi

      - name: Install clang
        if: matrix.compiler == 'clang'
        run: |
          sudo apt-get update
          sudo apt-get install -y clang

      - name: Cache ccache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ccache
          key: ${{ runner.os }}-${{ matrix.compiler }}-${{ matrix.build_type }}-ccache-${{ hashFiles('CMakeLists.txt', 'src/**/*.cpp', 'include/**/*.hpp') }}
          restore-keys: |
            ${{ runner.os }}-${{ matrix.compiler }}-${{ matrix.build_type }}-ccache-
            ${{ runner.os }}-${{ matrix.compiler }}-

      - name: Configure
        run: |
          cmake -S . -B "$BUILD_DIR" \
            -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache

      - name: Build
        run: cmake --build "$BUILD_DIR" --config ${{ matrix.build_type }}

      - name: Test
        run: ctest --test-dir "$BUILD_DIR" --output-on-failure --build-config ${{ matrix.build_type }}

  rowmajor:
    name: rowmajor constant-time
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        build_type: [Release]
    env:
      BUILD_DIR: build/gcc-${{ matrix.build_type }}-rowmajor
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup compiler
        run: |
          echo "CC=gcc" >> "$GITHUB_ENV"
          echo "CXX=g++" >> "$GITHUB_ENV"

      - name: Cache ccache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ccache
          key: ${{ runner.os }}-gcc-${{ matrix.build_type }}-rowmajor-ccache-${{ hashFiles('CMakeLists.txt', 'src/**/*.cpp', 'include/**/*.hpp') }}
          restore-keys: |
            ${{ runner.os }}-gcc-${{ matrix.build_type }}-rowmajor-ccache-
            ${{ runner.os }}-gcc-

      - name: Configure (rowmajor)
        run: |
          cmake -S . -B "$BUILD_DIR" \
            -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache \
            -DCUBE96_LAYOUT=rowmajor \
            -DCUBE96_FORCE_CONSTANT_TIME=ON

      - name: Build (rowmajor)
        run: cmake --build "$BUILD_DIR" --config ${{ matrix.build_type }}

      - name: Test (rowmajor labeled subsets)
        run: >-
          ctest --test-dir "$BUILD_DIR" --output-on-failure --build-config
          ${{ matrix.build_type }} -L "KAT|PERM|HKDF|CLI|CT"
